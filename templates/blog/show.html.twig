{% extends 'layout.html.twig' %}

{% block body %}
    <div class='container'>
        <p>
        {{ blog.title }}
        </p>
        <p>
        {{ blog.description }}
        </p>
    </div>
    <div class="container">
        <a href="{{ path('blog_list') }}">back</a>
    </div>
{% endblock %}